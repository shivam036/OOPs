#include <iostream>
using namespace std;

int main()
{
    int n, sum = 0;
    for (int i = 1; i <= 100; ++i) 
    {
        sum += i;
    }
    cout << "Sum of FIrst 100 Natural numbers = " << sum;
    return 0;
}
